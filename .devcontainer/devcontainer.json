{
  "name": "ansible-lab",

  "build": {
    "dockerfile": "Dockerfile"
  },

  "remoteUser": "vscode",

  "mounts": [
    "source=/home/daniel-bighelini/.ssh/agent,target=/ssh-agent,type=bind"
  ],

  "remoteEnv": {
    "SSH_AUTH_SOCK": "/ssh-agent/s.BShvyfOOSt.agent.HJadv8zTsg"
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "redhat.ansible",
        "ms-python.python",
        "ms-vscode.makefile-tools"
      ]
    }
  }
}