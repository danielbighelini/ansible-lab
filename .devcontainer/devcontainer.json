{
  "name": "ansible-lab",

  "build": {
    "dockerfile": "Dockerfile"
  },

  "remoteUser": "vscode",

  "mounts": [
    "source=${localEnv:HOME}/.ssh/ssh-agent.sock,target=/ssh-agent,type=bind"
  ],

  "remoteEnv": {
    "SSH_AUTH_SOCK": "/ssh-agent"
  },

  "postCreateCommand": "ansible --version && ansible-lint --version",

  "customizations": {
    "vscode": {
      "extensions": [
        "redhat.ansible",
        "ms-python.python",
        "ms-vscode.makefile-tools"
      ]
    }
  }
}